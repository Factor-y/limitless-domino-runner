<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.factory.domino</groupId>
    <artifactId>limitless-domino-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>domino-web-designer</artifactId>
  <packaging>jar</packaging>

  <name>Domino Web Designer</name>
  <description>Web application, launched by DominoRunner, exposing REST APIs and a UI to browse Domino databases.</description>

  <dependencies>
    <!-- Javalin brings Jetty, Jackson and SLF4J with it; all of these are bundled into the
         fat jar and stay isolated behind the runner's parent-last class loader. -->
    <dependency>
      <groupId>io.javalin</groupId>
      <artifactId>javalin</artifactId>
      <exclusions>
        <!-- Brotli compression ships native binaries per platform. It is optional at
             runtime and Javalin degrades gracefully without it, so it is left out rather
             than adding a second set of native libraries to a process that already hosts
             the Domino runtime. -->
        <exclusion>
          <groupId>com.aayushatharva.brotli4j</groupId>
          <artifactId>brotli4j</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- Javalin declares these as optional, so they are not inherited transitively, yet it
         needs both at runtime: without Jackson every ctx.json() returns a 500, and without an
         SLF4J provider Jetty logs nothing. -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>2.17.2</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>2.0.16</version>
    </dependency>

    <!-- Service discovery over mDNS/Bonjour. -->
    <dependency>
      <groupId>org.jmdns</groupId>
      <artifactId>jmdns</artifactId>
      <version>3.6.1</version>
    </dependency>

    <!-- Swagger UI assets, served from the fat jar so the API docs work offline.
         The version appears in the resource path, so DominoWebDesigner.SWAGGER_UI_VERSION
         must be kept in step with this. -->
    <dependency>
      <groupId>org.webjars</groupId>
      <artifactId>swagger-ui</artifactId>
      <version>5.25.3</version>
    </dependency>

    <!-- Domino JNX and the runner API are PROVIDED, not bundled. The runner's class loader
         shares these packages with the parent on purpose: a second copy of the Domino
         classes in this jar would bind the native libraries twice and break the process.
         Keeping them provided means the fat jar cannot even contain them. -->
    <dependency>
      <groupId>com.hcl.domino</groupId>
      <artifactId>domino-jnx-jna-${jnx.release}</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.factory.domino</groupId>
      <artifactId>domino-runner</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>

      <!-- Fat jar: everything except the provided Domino/runner classes.
           ServicesResourceTransformer is required here too — Jetty resolves parts of itself
           through META-INF/services. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>com.factory.domino.designer.DominoWebDesigner</mainClass>
                </transformer>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>module-info.class</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
